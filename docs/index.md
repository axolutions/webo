# webo documentation

- **[authentication.md](authentication.md)** — the two modes, the two kinds of
  credential, and how to get one.
- **[mcp.md](mcp.md)** — the 19 tools, 3 prompts and 2 resources, and the
  limits on what an agent can change.
- **[api.md](api.md)** — the HTTP API, which is the same contract the panel
  and the MCP server both consume.

The server itself carries a runbook — how the machine is put together and the
lessons that cost a broken deploy to learn. Read it as the MCP resource
`webo://runbook`.
